# sentinel_project_root/test/tests/__init__.py
# This file makes 'tests' a Python package. Useful for organizing tests and
# potentially for pytest discovery, though pytest is often smart enough without it
# if the directory name starts with 'test' or 'tests'.
